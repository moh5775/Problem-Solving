#include<stdio.h>

int main()
{
   int n,i,x;
   scanf("%d",&n);
   int sum=0;
   for(i=1;i<=n;i++)
   {
       scanf("%d",&x);
       sum=sum+x;
   }
   if(sum==0)
    printf("EASY\n");
   else
    printf("HARD\n");
}
