#include<stdio.h>
int main ()
{
    int x,y,z,p,q,r;
    scanf("%d %d %d",&x,&y,&z);
    p=abs(x-y)+abs(x-z);
    q=abs(y-x)+abs(y-z);
    r=abs(z-x)+abs(z-y);
    int min=p;
    if(q<min)
    min=q;
    if(r<min)
    min=r;
    printf("%d\n",min);
}