#include<stdio.h>
#include<math.h>
int main()
{
    long long n;
    int i;
    scanf("%lld",&n);
    long long sum=0;
    if(n%2==0)
        sum=(n/2);
    else
        sum=-(n+1)/2;
    printf("%lld\n",sum);
}
