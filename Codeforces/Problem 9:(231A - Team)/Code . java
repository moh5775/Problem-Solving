#include<stdio.h>

int main()
{
   int n,i,x,y,z;
   scanf("%d",&n);
   int count=0;
   for(i=1;i<=n;i++)
   {
       scanf("%d %d %d",&x,&y,&z);
       int sum=0;
       if(x+y+z>=2)
        count++;
   }
   printf("%d",count);
}
