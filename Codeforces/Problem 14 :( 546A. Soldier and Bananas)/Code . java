#include<stdio.h>
int main()
{
    int k,n,w,i;
    scanf("%d %d %d",&k,&n,&w);
    int total=0;
    for(i=1; i<=w; i++)
    {
        total=total+k*i;
    }
    int need = total - n;
    if(need>=0)
        printf("%d\n",need);
    else
        printf("0\n");
}
